export const HOST_DAEMON_PROTOCOL_VERSION = 128 as const;

/** Absolute ceiling for executable host-plugin bundles sent to a daemon. */
export const PLUGIN_HOST_ARTIFACT_MAX_BYTES = 16 * 1024 * 1024;
