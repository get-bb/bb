packages:
  - "packages/*"
  - "apps/*"
  - "tests/*"
  - "plugins/*"
  - "official-plugins/*"
  # Official and example plugins are workspace members so their declared
  # dependencies are installed into each plugin's own node_modules — plugins
  # bring their own dependencies.
  - "examples/plugins/*"
