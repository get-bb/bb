// Moved to @bb/client-core (shared with the native app); re-exported here so web imports keep resolving.
export { PERMISSION_MODE_OPTIONS } from "@bb/client-core";
export type { PermissionModeOption } from "@bb/client-core";
