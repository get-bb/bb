# legacy fixture

Original minimal KiCad 5 header fragment authored for WP-73. It is intentionally
stored under a `.kicad_sch` name so `parseProject` reaches the version gate and
proves that legacy Version 4 input is rejected explicitly rather than parsed.
