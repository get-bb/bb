import type { PluginAppBuilder } from "@bb/plugin-sdk/app";
import type { AppContext } from "../../lib/app-context.js";

export function registerBomApp(_app: PluginAppBuilder, _ctx: AppContext): void {
  // TODO(L5): BOM frontend registration. See WP-42, WP-45, and WP-46.
}
