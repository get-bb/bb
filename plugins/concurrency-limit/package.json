{
  "name": "bb-plugin-concurrency-limit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Limit how many threads run at once, in total and per host. Every running thread counts, including child and plugin-spawned ones.",
  "engines": {
    "bb": ">=0.0"
  },
  "bb": {
    "name": "Concurrency limit",
    "description": "Limit how many threads run at once, in total and per host. Every running thread counts, including child and plugin-spawned ones.",
    "branding": {
      "icon": "Limitation"
    },
    "server": "./server.ts"
  },
  "keywords": [
    "bb-plugin"
  ],
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "vitest": "^4.1.1"
  }
}
