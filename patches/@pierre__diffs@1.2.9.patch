diff --git a/dist/renderers/DiffHunksRenderer.js b/dist/renderers/DiffHunksRenderer.js
--- a/dist/renderers/DiffHunksRenderer.js
+++ b/dist/renderers/DiffHunksRenderer.js
@@ -150,6 +150,9 @@ var DiffHunksRenderer = class {
 		this.renderCache ??= {
 			diff,
 			highlighted: !massiveDiff && !isDiffPlainText(diff),
+			// Strict Mode can hydrate a retained plain AST. The worker result must
+			// repaint that adopted DOM even though hydration considers it highlighted.
+			highlightPending: cache == null && !massiveDiff && !isDiffPlainText(diff),
 			options,
 			result: massiveDiff ? void 0 : cache?.result,
 			renderRange: void 0
@@ -283,6 +284,6 @@ var DiffHunksRenderer = class {
 	onHighlightSuccess(diff, result, options, highlighted = true) {
 		if (this.renderCache == null) return;
-		const triggerRenderUpdate = !this.renderCache.highlighted || !areDiffRenderOptionsEqual(this.renderCache.options, options) || !areDiffTargetsEqual(this.renderCache.diff, diff);
+		const triggerRenderUpdate = this.renderCache.highlightPending === true || !this.renderCache.highlighted || !areDiffRenderOptionsEqual(this.renderCache.options, options) || !areDiffTargetsEqual(this.renderCache.diff, diff);
 		this.renderCache = {
 			diff,
 			options,
