{
  "name": "bb-plugin-bench-stream-provider",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "bb": ">=0.0",
    "bbPluginSdk": ">=0.4.3"
  },
  "bb": {
    "name": "Bench stream provider (benchmark harness)",
    "description": "A deterministic provider that streams scripted Markdown at a controlled rate for the thread streaming render benchmark. Not a product plugin.",
    "branding": {
      "icon": "Zap"
    },
    "server": "./server.ts",
    "host": "./host.ts"
  },
  "exports": {
    "./fixtures": {
      "source": "./src/fixtures/index.ts",
      "types": "./src/fixtures/index.ts",
      "default": "./src/fixtures/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@get-bb/plugin-sdk": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-7": "npm:typescript@^7.0.2",
    "vitest": "^4.1.1"
  }
}
